Karen Cheng

Both functions in getbest.py has been explicitly tested to find the correct top student in a CSV file independent of the order of the columns.

There were custom examples added to peform the test multiple times to ensure the functionality of getbest.py

To perform the test:
1. Run and debug the file test_getbest.py
2. Look for the message "All Testcases Passed!" in the terminal if testcase has been successful.
3. If the message "Test Failed" shows up in the terminal, the testcase has failed.